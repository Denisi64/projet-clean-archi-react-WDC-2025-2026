// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"  // <--- pas de "output"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Ping {
  id Int @id @default(autoincrement())
}